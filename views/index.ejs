<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta
      name="description"
      content="Professional blog by <%= profile.firstName %> <%= profile.lastName %> featuring tech insights and articles"
    />
    <title>
      <%= profile.firstName || 'Dusk' %> <%= profile.lastName || 'Blog' %> |
      Tech Insights
    </title>

    <!-- Structured Data -->

    <style>
      :root {
        --primary: #f59e0b;
        --primary-hover: #d97706;
        --secondary: #4f46e5;
        --text-primary: #1f2937;
        --text-secondary: #6b7280;
      }

      @font-face {
        font-family: "Inter";
        font-style: normal;
        font-weight: 400;
        font-display: swap;
        src: local("Inter Regular"),
          url("/fonts/Inter-Regular.woff2") format("woff2");
      }

      @font-face {
        font-family: "Inter";
        font-style: normal;
        font-weight: 600;
        font-display: swap;
        src: local("Inter SemiBold"),
          url("/fonts/Inter-SemiBold.woff2") format("woff2");
      }

      body {
        font-family: "Inter", -apple-system, BlinkMacSystemFont, sans-serif;
        line-height: 1.6;
        color: var(--text-primary);
        -webkit-font-smoothing: antialiased;
        text-rendering: optimizeLegibility;
      }

      .prose {
        max-width: 65ch;
      }

      .prose a {
        color: var(--primary);
        text-decoration: none;
        font-weight: 500;
        transition: color 0.2s ease;
      }

      .prose a:hover {
        color: var(--primary-hover);
        text-decoration: underline;
      }

      .gradient-text {
        background-image: linear-gradient(
          45deg,
          var(--primary),
          var(--secondary)
        );
        -webkit-background-clip: text;
        background-clip: text;
        color: transparent;
      }

      .card-hover {
        transition: transform 0.3s ease, box-shadow 0.3s ease;
      }

      .card-hover:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1);
      }

      .fade-in {
        animation: fadeIn 0.5s ease-out forwards;
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(10px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .delay-100 {
        animation-delay: 0.1s;
      }
      .delay-200 {
        animation-delay: 0.2s;
      }
      .delay-300 {
        animation-delay: 0.3s;
      }

      /* Accessibility focus styles */
      a:focus,
      button:focus {
        outline: 2px solid var(--primary);
        outline-offset: 2px;
      }
    </style>
  </head>
  <body class="bg-gray-50">
  <%- include('./partials/header.ejs') %>

    <!-- Skip to content link for accessibility -->
    <a
      href="#main-content"
      class="sr-only focus:not-sr-only fixed top-2 left-2 bg-white p-2 z-50"
      >Skip to content</a
    >

    <!-- Navigation -->

  

    <!-- Main Content -->
    <main id="main-content" class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <!-- Hero Section -->
    
      <section
        class="bg-gradient-to-r from-amber-500 to-amber-600 rounded-xl shadow-lg overflow-hidden mb-12 fade-in"
      >
  <%- include('./partials/search.ejs') %>

        <div
          class="max-w-7xl mx-auto py-12 px-6 sm:py-16 sm:px-8 lg:px-10 lg:py-20"
        >
   

        </div>
          <div class="text-center w-full">
            <h1
                class=" w-full text-center text-3xl font-extrabold tracking-tight text-purple-800 sm:text-5xl lg:text-2xl"
            >
             LATEST BLOGS TRENDING ON!
            </h1>

            <p class="mt-6 max-w-3xl mx-auto text-xl text-amber-100">
              Expert perspectives on modern technology, development, and
              industry trends
            </p>
            <div class="mt-10 mb-20 flex justify-center space-x-4">
              <a
                href="/blogs"
                class="inline-flex items-center px-6 py-3 border border-transparent text-base font-medium rounded-md shadow-sm text-white bg-black hover:bg-gray-500"
              >
                Browse Articles
              </a>
              <a
                href="/about"
                class="inline-flex items-center px-6 py-3 border border-transparent text-base font-medium rounded-md shadow-sm tex-white bg-yellow-700 bg-opacity-60 hover:bg-opacity-70"
              >
                Learn About Me
              </a>
            </div>
          </div>
        </div>
      </section>

      <!-- Featured Articles -->
  <!-- Featured Articles -->
    <section class="mb-16 fade-in delay-100">
      <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center mb-8">
        <h2 class="text-2xl font-bold text-gray-900 dark:text-white mb-4 sm:mb-0">
          Featured Articles
          <span class="text-sm text-amber-500 ml-2 bg-amber-100 dark:bg-amber-900/30 px-2 py-1 rounded-full">Updated Daily</span>
        </h2>
        <a href="/blog" class="inline-flex items-center text-amber-600 hover:text-amber-700 dark:text-amber-400 dark:hover:text-amber-300 font-medium group transition-colors duration-200">
          View all articles
          <svg class="ml-2 h-4 w-4 group-hover:translate-x-1 transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
          </svg>
        </a>
      </div>

      <div class="grid gap-8 md:grid-cols-2 lg:grid-cols-3">
        <% blogs.slice(0, 3).forEach(blog => { %>
        <article  class="bg-whitedark:bg-gray-800 rounded-xl shadow-md overflow-hidden card-hover group">
          <a href="/blog/<%= blog._id %>" class="block">
            <div class="aspect-w-10 aspect-h-9 relative">
              <img style="height: 200px;"
                src="<%- blog.image %>"
                alt="<%= blog.title %>"
                width="200"
                height="200"
                class="w-full object-cover"
                
              />

              <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-transparent to-transparent p-4 flex flex-col justify-end">
                <span class="inline-block bg-amber-500 text-white text-xs px-3 py-1 rounded-full uppercase font-semibold tracking-wide mb-2 self-start">
                  <%= blog.category  %>
                </span>
                
                <div class="lex items-center text-sm text-gray-600" >
                  <time class="text-white" datetime="<%= blog.date.toISOString() %>" >
                    <%= blog.date.toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) %>
                  </time>
                  
                  <span class="mx-5 text-white">•</span>
                  <span class="text-white" ><%- Math.ceil(blog.content.length / 200) %> min read</span>
                </div>
              </div>
            </div>
            <div class="p-4">
              <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-3 leading-tight line-clamp-2">
                <%= blog.title %>
              </h3>
              <p class="text-gray-600 dark:text-gray-300 mb-4 line-clamp-3 ">
                <%- blog.content.substring(20, 150) %>...
              </p>
              <div class="flex items-center">
                <% if (blog.author) { %>
                  <img 
                    src="<%= blog.author.profileImage || '/uploads/default-profile.jpg' %>" 
                    alt="<%= blog.author.firstName || 'Author' %>" 
                    class="w-8 h-8 rounded-full  mr-3"
                  
                  />
                  <div>
                    <p class="text-sm font-medium text-gray-900 dark:text-white"><%= blog.author.firstName %> <%= blog.author.lastName %></p>
                    <p class="text-xs text-gray-500 dark:text-gray-400"><%= blog.author.title || 'Writer' %></p>
                  </div>
                <% } else { %>
                  <p class="text-sm font-medium text-red-500">Unknown Author</p>
                <% } %>
              </div>
              <p class="text-gray-500 text-sm mt-5">👁️ <%= blog.views %> views</p>
              <span class="text-gray-500 text-sm  mt-5 ">❤️ <%= blog.likes.length %> like</span>
            </div>
          </a>
    

        </article>
        <% }) %>
      </div>
    </section>

     <!-- All Articles -->
  
<section class="fade-in delay-200">
  <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center mb-8">
    <h2 class="text-2xl font-bold text-gray-900 dark:text-white mb-4 sm:mb-0">
      Latest Articles
      <span class="text-sm text-gray-500 dark:text-gray-400 font-normal ml-2"><%= blogs.length %> total articles</span>
    </h2>
    <div class="flex items-center space-x-4">
      <div class="relative">
        <label for="sort-by" class="sr-only">Sort by</label>
        <select
          id="sort-by"
          class="appearance-none bg-white dark:bg-gray-800 dark:text-white border border-gray-300 dark:border-gray-700 rounded-md pl-3 pr-8 py-2 text-gray-700 dark:text-gray-300 focus:outline-none focus:ring-2 focus:ring-amber-500 focus:border-amber-500 cursor-pointer"
        >
          <option value="latest">Latest</option>
          <option value="popular">Most Popular</option>
          <option value="trending">Trending</option>
          <option value="featured">Featured</option>
        </select>
        <div class="pointer-events-none absolute inset-y-0 right-0 flex items-center px-2 text-gray-700 dark:text-gray-300">
          <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
          </svg>
        </div>
      </div>
      <div class="flex border border-gray-300 dark:border-gray-700 rounded-md divide-x divide-gray-300 dark:divide-gray-700">
        <button
          id="grid-view"
          class="p-2 text-gray-500 dark:text-gray-400 hover:text-amber-600 dark:hover:text-amber-400 focus:outline-none"
          aria-label="Grid view"
        >
          <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2V6zM14 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2V6zM4 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2v-2zM14 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2v-2z"></path>
          </svg>
        </button>
        <button 
          id="list-view" 
          class="p-2 text-amber-600 dark:text-amber-400 focus:outline-none"
          aria-label="List view"
        >
          <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
          </svg>
        </button>
      </div>
    </div>
  </div>

  <div id="articles-container" class="space-y-2">
    <% blogs.slice(3).forEach(blog => { %>
    <article class="bg-white dark:bg-gray-800 rounded-xl shadow-md overflow-hidden card-hover">
      <div class="md:flex">
        <div class="md:flex-shrink-0 md:w-1/3">
          <a href="/blog/<%= blog._id %>" class="block w-full h-full">
            <img style="height: 250px; width: 100%;"
              src="<%= blog.image %>"
              alt="<%= blog.title %>"
              class="object-cover transition-transform duration-500 hover:scale-105"
            />
          </a>
        </div>
        <div class="p-6 md:w-2/3">
          <div class="flex flex-wrap items-center text-xs text-gray-500 dark:text-gray-400 mb-3 gap-2">
            <span class="inline-flex items-center bg-gray-100 dark:bg-gray-700 rounded-full px-3 py-1">
              <%= blog.category %>
            </span>
            <time datetime="<%= blog.date.toISOString() %>">
              <%= blog.date.toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' }) %>
            </time>
            <span class="flex items-center">
              <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
              <% Math.ceil(blog.content.length / 200) %> min read
            </span>
            <% if (blog.views > 1000) { %>
            <span class="flex items-center text-amber-600 dark:text-amber-400">
              <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
              </svg>
              <%= (blog.views / 1000).toFixed(1) %>k views
            </span>
            <% } %>
          </div>
          <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-3 line-clamp-2">
            <a href="/blog/<%= blog._id %>" class="hover:text-amber-600 dark:hover:text-amber-400 transition-colors duration-200">
              <%= blog.title %>
            </a>
          </h3>
          <p class="text-gray-600 dark:text-gray-300 mb-4 line-clamp-3">
            <%- blog.content.substring(0, 150) %>...
          </p>
          <div class="flex items-center justify-between">
            <div class="flex items-center">
              <img
                src="<%= blog.author.profileImage || '/uploads/default-profile.jpg' %>"
                alt="<%= blog.author %>"
                width="32"
                height="32"
                class="w-8 h-8 rounded-full object-cover mr-3"
              />
              <div>
                <p class="text-sm font-medium text-gray-900 dark:text-white"><%= blog.author.firstName %> <%= blog.author.lastName %></p>
                <% if (blog.authorRole) { %>
                <p class="text-xs text-gray-500 dark:text-gray-400"><%= blog.authorRole %></p>
                <% } %>
              </div>
            </div>
            <a
              href="/blog/<%= blog._id %>"
              class="inline-flex items-center text-amber-600 hover:text-amber-700 dark:text-amber-400 dark:hover:text-amber-300 text-sm font-medium group transition-colors duration-200"
            >
              Read more
              <svg
                class="ml-1 h-4 w-4 group-hover:translate-x-1 transition-transform duration-200"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
              </svg>
            </a>
          </div>
          <p class="text-gray-500 text-sm mt-5">👁️ <%= blog.views %> views</p>
        </div>
      </div>
    </article>
    <% }) %>
  </div>

  <!-- Pagination -->

  
</section>

    <!-- Newsletter CTA -->
    <section class="mt-20 bg-gradient-to-r from-amber-50 to-amber-100 dark:from-gray-800 dark:to-gray-900 rounded-xl p-8 md:p-12 shadow-inner fade-in delay-300 relative overflow-hidden">
      <div class="absolute inset-0 opacity-10 dark:opacity-5 bg-[url('/images/pattern.svg')]"></div>
      <div class="max-w-3xl mx-auto text-center relative">
        <h2 class="text-2xl md:text-3xl font-bold text-gray-900 dark:text-white mb-4">
          Stay Updated with Our Newsletter
        </h2>
        <p class="text-gray-700 dark:text-gray-300 mb-6 max-w-2xl mx-auto">
        </p>
        <form class="flex flex-col sm:flex-row gap-3 max-w-md mx-auto">
          <label for="email" class="sr-only">Email address</label>
          <input
            id="email"
            type="email"
            placeholder="Your email address"
            required
            class="flex-grow px-4 py-3 rounded-md border border-gray-300 dark:border-gray-700 bg-white dark:bg-gray-800 dark:text-white focus:outline-none focus:ring-2 focus:ring-amber-500 focus:border-amber-500"
          />
          <button
            type="submit"
            class="px-6 py-3 bg-amber-500 hover:bg-amber-600 text-white font-medium rounded-md transition-colors shadow-md hover:shadow-lg focus:outline-none focus:ring-2 focus:ring-amber-500 focus:ring-offset-2"
          >
            Subscribe
          </button>
        </form>
        <p class="text-xs text-gray-500 dark:text-gray-400 mt-3">
          We respect your privacy. Unsubscribe at any time. No spam, ever.
        </p>
      </div>
    </section>
  </main>

  <%- include('./partials/footer.ejs') %>

  <!-- Back to Top Button -->
  <button
    id="back-to-top"
    class="fixed bottom-8 right-8 bg-amber-500 text-black p-3 rounded-full shadow-lg opacity-0 invisible transition-all duration-300 hover:bg-amber-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-amber-500 z-50"
    aria-label="Back to top"
  >
    <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 10l7-7m0 0l7 7m-7-7v18"></path>
    </svg>
  </button>

  <!-- Loading overlay -->
  <div id="loading-overlay" class="fixed inset-0 bg-white dark:bg-gray-900 z-50 flex items-center justify-center transition-opacity duration-300 opacity-0 pointer-events-none">
    <div class="animate-pulse flex flex-col items-center">
      <div class="w-16 h-16 bg-amber-500 rounded-full mb-4"></div>
      <div class="h-4 bg-gray-200 dark:bg-gray-700 rounded w-32"></div>
    </div>
  </div>


  <script src="/js/main.js"></script>