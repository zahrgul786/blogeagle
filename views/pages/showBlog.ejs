<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
</head>
<body>
  <div class="container mx-auto p-4">
    <h1 class="text-3xl font-semibold"><%= blog.title %></h1>
    <div class="mt-4 text-gray-600">
      <p>Written by <%= blog.author %> on <%= blog.date.toLocaleDateString() %></p>
    </div>
    <div class="mt-6">
      <img src="<%= blog.image %>" alt="<%= blog.title %>" class="max-w-full h-auto rounded-lg">
    </div>
    <div class="mt-6">
      <p><%- blog.content %></p>
    </div>

    <!-- Check if the logged-in user is the author -->
    <% if (user && user.username === blog.author) { %>
      <div class="flex gap-4 mt-6">
        <!-- Edit Button -->
        <a href="/blog/<%= blog.id %>/edit" class="bg-yellow-500 text-white px-4 py-2 rounded shadow">
          ✏️ Edit
        </a>
        
        <!-- Delete Button -->
        <form action="/blog/<%= blog.id %>/delete" method="POST" onsubmit="return confirm('Are you sure?');">
          <button type="submit" class="bg-red-600 text-white px-4 py-2 rounded shadow">
            🗑️ Delete
          </button>
        </form>
      </div>
    <% } %>
  </div>
</body>
</html>
